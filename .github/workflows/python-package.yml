# This workflow will install Python dependencies, run tests and lint with a variety of Python versions
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Python package

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.8", "3.9", "3.10"]

    steps:
    - name: Checkout reptar
      uses: actions/checkout@v3
    
    - name: Checkout reptar-data
      uses: actions/checkout@v3
      with:
        repository: 'aalexmmaldonado/reptar-data'
        lfs: true
        path: examples/reptar-data
    
    - uses: conda-incubator/setup-miniconda@v2
      with:
        miniconda-version: "latest"
        auto-update-conda: true
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      shell: bash -el {0}
      run: |
        conda create -y --name reptar-test
        conda activate reptar-test
        conda install -y python=${{ matrix.python-version }} psi4 -c psi4
        pip install --upgrade pip
        pip install numpy exdir>=0.4.2 cclib>=1.7.0 scipy qcelemental>=0.25.1 pyyaml ase ray
        pip install pytest pytest-order pytest-dependency
    
    - name: Install reptar
      shell: bash -el {0}
      run: pip install .
    
    - name: Test with pytest
      shell: bash -el {0}
      run: pytest ./
